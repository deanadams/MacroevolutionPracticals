anova(PGLS.reg)
plot(PGLS.reg,type = "regression", reg.type = "RegScore", predictor = rdf$svl)
## ----Ordination_prep ----
library(geomorph)
data(plethspecies)
Y.gpa <- gpagen(plethspecies$land, print.progress = F)    #GPA-alignment
## ----PCA_example ----
PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy)
plot(PCA, main = "PCA", pch = 21, bg = "red")
## ----Phylomorph_example ----
plot(PCA, phylo = TRUE, pch = 21, bg = "red",
main = "Phylomorphospace")
## ----phyloPCA_example ----
phylo.PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy, GLS = TRUE)
plot(phylo.PCA, phylo = TRUE, pch = 21, bg="red",
main = "phylogenetic PCA")
## ----PaCA_example ----
PaCA.gls <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy,
align.to.phy = TRUE, GLS = TRUE)
plot(PaCA.gls, phylo = TRUE, pch = 21, bg = "red",
main = "PaCA: Phylogenetically-Aligned Component Analysis")
## ----read_data ----
library(RRPP)
library(geiger)
library(phytools)
tree.best<-read.nexus("../data/Consensus of 1000 salamander trees.nex") #Maximum Credible Tree
plethdata<-read.csv("../data/meandata-CinGlutOnly.csv",row.names=1, header=TRUE )
svl<-plethdata[,2];names(svl)<-row.names(plethdata)
groups<-as.factor(plethdata[,1]); names(groups)<-row.names(plethdata)
Y<-as.matrix(plethdata[,c(3:8)])  ##This is our multivariate data
plethtree<-treedata(phy = tree.best,data = plethdata, warnings = FALSE)$phy
plot(plethtree)
axisPhylo(1)
PCA<-gm.prcomp(Y,phy=plethtree)   #principal components of Y
plot(PCA, pch=21,bg="black") ##we can put the PCA directly into the plot function
## ----phylo_reg----
PhyCov <- vcv.phylo(plethtree) #generate phylogenetic covariance matrix
rdf <- rrpp.data.frame(Y=Y, svl=svl,groups=groups,Cov = PhyCov)  #RRPP data frame
PGLS.reg <- lm.rrpp(Y ~ svl, Cov = PhyCov, data = rdf, iter = 999,
print.progress = FALSE,) # randomize residuals
anova(PGLS.reg)
## ---- phylo_ANOVA----
PGLS.aov<-lm.rrpp(Y ~ groups, Cov = PhyCov, data = rdf, iter = 999, print.progress = FALSE) # randomize residuals
anova(PGLS.aov)  #no difference once phylogeny considered
## ---- phylo_PLS ----
PLS.Y <- phylo.integration(A = Y[,1:3], A2 = Y[,4:6], phy= plethtree, print.progress = FALSE)
summary(PLS.Y)
plot(PLS.Y)
## ----phylo_signal----
PS.shape <- physignal(A=Y,phy=plethtree,iter=999, print.progress = FALSE)
summary(PS.shape)
plot(PS.shape)
## ----evol_rates----
ER<-compare.evol.rates(A=Y, phy=plethtree,gp=groups,iter=999, print.progress = FALSE)
summary(ER)   #significantly higher rate of morphological evolution 'large' Plethodon
plot(ER)
var.gp <- c("B","A","A","B","B","B")  #head (A) vs. body (B)
EMR<-compare.multi.evol.rates(A=Y,gp=var.gp, Subset=TRUE, phy= plethtree,iter=999, print.progress = FALSE)
summary(EMR) #Limb traits evolve faster than head traits
plot(EMR)
## ----pleth_read ----
data("plethspecies")
pleth_tree<-plethspecies$phy
landmarks<-plethspecies$land
procD_landmarks <- gpagen(landmarks)
## ----Ordination_prep ----
library(geomorph)
data(plethspecies)
Y.gpa <- gpagen(plethspecies$land, print.progress = F)    #GPA-alignment
pairs(two.d.array(Y.gpa$coords))
pairs(two.d.array(Y.gpa$coords))
pairs(two.d.array(Y.gpa$coords[,1:6]))
pairs(two.d.array(Y.gpa$coords)[,1:6])
## ----PCA_example ----
PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy)
## ----PCA_example ----
PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy)
plot(PCA, main = "PCA", pch = 21, bg = "red")
## ----Phylomorph_example ----
plot(PCA, phylo = TRUE, pch = 21, bg = "red",
main = "Phylomorphospace")
## ----phyloPCA_example ----
phylo.PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy, GLS = TRUE)
plot(phylo.PCA, phylo = TRUE, pch = 21, bg="red",
main = "phylogenetic PCA")
## ----PaCA_example ----
PaCA.gls <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy,
align.to.phy = TRUE, GLS = TRUE)
plot(PaCA.gls, phylo = TRUE, pch = 21, bg = "red",
main = "PaCA: Phylogenetically-Aligned Component Analysis")
## ----read_data ----
library(RRPP)
library(geiger)
library(phytools)
tree.best<-read.nexus("../data/Consensus of 1000 salamander trees.nex") #Maximum Credible Tree
plethdata<-read.csv("../data/meandata-CinGlutOnly.csv",row.names=1, header=TRUE )
svl<-plethdata[,2];names(svl)<-row.names(plethdata)
groups<-as.factor(plethdata[,1]); names(groups)<-row.names(plethdata)
Y<-as.matrix(plethdata[,c(3:8)])  ##This is our multivariate data
plethtree<-treedata(phy = tree.best,data = plethdata, warnings = FALSE)$phy
plot(plethtree)
axisPhylo(1)
plot(plethtree, show.tip.label = F)
axisPhylo(1)
View(Y)
PCA<-gm.prcomp(Y,phy=plethtree)   #principal components of Y
plot(PCA, pch=21,bg="black") ##we can put the PCA directly into the plot function
cor(Y)
## ----phylo_reg----
PhyCov <- vcv.phylo(plethtree) #generate phylogenetic covariance matrix
rdf <- rrpp.data.frame(Y=Y, svl=svl,groups=groups,Cov = PhyCov)  #RRPP data frame
class(rdf)
rdf$Y
rdf$svl
rdf$groups
rdf$Cov
PGLS.reg <- lm.rrpp(Y ~ svl, Cov = PhyCov, data = rdf, iter = 999,
print.progress = FALSE,) # randomize residuals
anova(PGLS.reg)
## ---- phylo_ANOVA----
PGLS.aov<-lm.rrpp(Y ~ groups, Cov = PhyCov, data = rdf, iter = 999, print.progress = FALSE) # randomize residuals
anova(PGLS.aov)  #no difference once phylogeny considered
groups
anova(PGLS.aov)  #no difference once phylogeny considered
## ---- phylo_PLS ----
PLS.Y <- phylo.integration(A = Y[,1:3], A2 = Y[,4:6], phy= plethtree, print.progress = FALSE)
summary(PLS.Y)
plot(PLS.Y)
## ----phylo_signal----
PS.shape <- physignal(A=Y,phy=plethtree,iter=999, print.progress = FALSE)
summary(PS.shape)
plot(PS.shape)
groups
## ----evol_rates----
ER<-compare.evol.rates(A=Y, phy=plethtree,gp=groups,iter=999, print.progress = FALSE)
summary(ER)   #significantly higher rate of morphological evolution 'large' Plethodon
plot(ER)
var.gp <- c("B","A","A","B","B","B")  #head (A) vs. body (B)
var.gp
colnames(Y)
EMR<-compare.multi.evol.rates(A=Y,gp=var.gp, Subset=TRUE, phy= plethtree,iter=999, print.progress = FALSE)
summary(EMR) #Limb traits evolve faster than head traits
plot(EMR)
## ----Ordination_prep ----
library(geomorph)
data(plethspecies)
Y.gpa <- gpagen(plethspecies$land, print.progress = F)    #GPA-alignment
## ----PCA_example ----
PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy)
plot(PCA, main = "PCA", pch = 21, bg = "red")
## ----Phylomorph_example ----
plot(PCA, phylo = TRUE, pch = 21, bg = "red",
main = "Phylomorphospace")
## ----phyloPCA_example ----
phylo.PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy, GLS = TRUE)
plot(phylo.PCA, phylo = TRUE, pch = 21, bg="red",
main = "phylogenetic PCA")
## ----PaCA_example ----
PaCA.gls <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy,
align.to.phy = TRUE, GLS = TRUE)
plot(PaCA.gls, phylo = TRUE, pch = 21, bg = "red",
main = "PaCA: Phylogenetically-Aligned Component Analysis")
## ----read_data ----
library(RRPP)
library(geiger)
library(phytools)
tree.best<-read.nexus("../data/Consensus of 1000 salamander trees.nex") #Maximum Credible Tree
plethdata<-read.csv("../data/meandata-CinGlutOnly.csv",row.names=1, header=TRUE )
svl<-plethdata[,2];names(svl)<-row.names(plethdata)
groups<-as.factor(plethdata[,1]); names(groups)<-row.names(plethdata)
Y<-as.matrix(plethdata[,c(3:8)])  ##This is our multivariate data
plethtree<-treedata(phy = tree.best,data = plethdata, warnings = FALSE)$phy
plot(plethtree)
axisPhylo(1)
PCA<-gm.prcomp(Y,phy=plethtree)   #principal components of Y
plot(PCA, pch=21,bg="black") ##we can put the PCA directly into the plot function
## ----phylo_reg----
PhyCov <- vcv.phylo(plethtree) #generate phylogenetic covariance matrix
rdf <- rrpp.data.frame(Y=Y, svl=svl,groups=groups,Cov = PhyCov)  #RRPP data frame
PGLS.reg <- lm.rrpp(Y ~ svl, Cov = PhyCov, data = rdf, iter = 999,
print.progress = FALSE,) # randomize residuals
anova(PGLS.reg)
## ---- phylo_ANOVA----
PGLS.aov<-lm.rrpp(Y ~ groups, Cov = PhyCov, data = rdf, iter = 999, print.progress = FALSE) # randomize residuals
anova(PGLS.aov)  #no difference once phylogeny considered
## ---- phylo_PLS ----
PLS.Y <- phylo.integration(A = Y[,1:3], A2 = Y[,4:6], phy= plethtree, print.progress = FALSE)
summary(PLS.Y)
plot(PLS.Y)
## ----phylo_signal----
PS.shape <- physignal(A=Y,phy=plethtree,iter=999, print.progress = FALSE)
summary(PS.shape)
plot(PS.shape)
## ----evol_rates----
ER<-compare.evol.rates(A=Y, phy=plethtree,gp=groups,iter=999, print.progress = FALSE)
summary(ER)   #significantly higher rate of morphological evolution 'large' Plethodon
plot(ER)
var.gp <- c("B","A","A","B","B","B")  #head (A) vs. body (B)
EMR<-compare.multi.evol.rates(A=Y,gp=var.gp, Subset=TRUE, phy= plethtree,iter=999, print.progress = FALSE)
summary(EMR) #Limb traits evolve faster than head traits
plot(EMR)
## ----pleth_read ----
data("plethspecies")
pleth_tree<-plethspecies$phy
landmarks<-plethspecies$land
procD_landmarks <- gpagen(landmarks)
procD_landmarks
## ----Ordination_prep ----
library(geomorph)
data(plethspecies)
Y.gpa <- gpagen(plethspecies$land, print.progress = F)    #GPA-alignment
plot(Y.gpa$coords)
plotAllSpecimens(Y.gpa$coords)
## ----PCA_example ----
PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy)
plot(PCA, main = "PCA", pch = 21, bg = "red")
## ----Phylomorph_example ----
plot(PCA, phylo = TRUE, pch = 21, bg = "red",
main = "Phylomorphospace")
PCA$anc.x
## ----phyloPCA_example ----
phylo.PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy, GLS = TRUE)
plot(phylo.PCA, phylo = TRUE, pch = 21, bg="red",
main = "phylogenetic PCA")
## ----PaCA_example ----
PaCA.gls <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy,
align.to.phy = TRUE, GLS = TRUE)
plot(PaCA.gls, phylo = TRUE, pch = 21, bg = "red",
main = "PaCA: Phylogenetically-Aligned Component Analysis")
tree.best<-read.nexus("../data/Consensus of 1000 salamander trees.nex") #Maximum Credible Tree
plethdata<-read.csv("../data/meandata-CinGlutOnly.csv",row.names=1, header=TRUE )
svl<-plethdata[,2];names(svl)<-row.names(plethdata)
groups<-as.factor(plethdata[,1]); names(groups)<-row.names(plethdata)
Y<-as.matrix(plethdata[,c(3:8)])  ##This is our multivariate data
plethtree<-treedata(phy = tree.best,data = plethdata, warnings = FALSE)$phy
## ----read_data ----
library(RRPP)
library(geiger)
library(phytools)
tree.best<-read.nexus("../data/Consensus of 1000 salamander trees.nex") #Maximum Credible Tree
plethdata<-read.csv("../data/meandata-CinGlutOnly.csv",row.names=1, header=TRUE )
svl<-plethdata[,2];names(svl)<-row.names(plethdata)
groups<-as.factor(plethdata[,1]); names(groups)<-row.names(plethdata)
Y<-as.matrix(plethdata[,c(3:8)])  ##This is our multivariate data
plot(tree.best, show.tip.label = FALSE)
plethtree<-treedata(phy = tree.best,data = plethdata, warnings = FALSE)$phy
plot(plethtree)
axisPhylo(1)
Y
PCA<-gm.prcomp(Y,phy=plethtree)   #principal components of Y
plot(PCA, pch=21,bg="black") ##we can put the PCA directly into the plot function
plot(PCA, pch=21,bg="black", phylo = TRUE) ##we can put the PCA directly into the plot function
plot(PCA, pch=21,bg="black", phylo = TRUE, time.plot = TRUE) ##we can put the PCA directly into the plot function
class(PCA)
geomorph:::plot.gm.prcomp
## ----phylo_reg----
PhyCov <- vcv.phylo(plethtree) #generate phylogenetic covariance matrix
rdf <- rrpp.data.frame(Y=Y, svl=svl,groups=groups,Cov = PhyCov)  #RRPP data frame
PGLS.reg <- lm.rrpp(Y ~ svl, Cov = PhyCov, data = rdf, iter = 999,
print.progress = FALSE,) # randomize residuals
anova(PGLS.reg)
PGLS.reg$ANOVA$SS
PGLS.reg$ANOVA$SS[[1]]
anova(PGLS.reg)
hist(PGLS.reg$ANOVA$SS)
Y
groups
## ---- phylo_ANOVA----
PGLS.aov<-lm.rrpp(Y ~ groups, Cov = PhyCov, data = rdf, iter = 999, print.progress = FALSE) # randomize residuals
anova(PGLS.aov)  #no difference once phylogeny considered
anova(lm.rrpp(Y ~ groups, data = rdf, iter = 999, print.progress = FALSE)) # randomize residuals
## ----phylo_signal----
PS.shape <- physignal(A=Y,phy=plethtree,iter=999, print.progress = FALSE)
summary(PS.shape)
plot(PS.shape)
## ----evol_rates----
ER<-compare.evol.rates(A=Y, phy=plethtree,gp=groups,iter=999, print.progress = FALSE)
summary(ER)   #significantly higher rate of morphological evolution 'large' Plethodon
plot(ER)
var.gp <- c("B","A","A","B","B","B")  #head (A) vs. body (B)
var.gp <- c("B","A","A","B","B","B")  #head (A) vs. body (B)
EMR<-compare.multi.evol.rates(A=Y,gp=var.gp, Subset=TRUE, phy= plethtree,iter=999, print.progress = FALSE)
summary(EMR) #Limb traits evolve faster than head traits
3.97 /.128
plot(EMR)
## ----Ordination_prep ----
library(geomorph)
data(plethspecies)
Y.gpa <- gpagen(plethspecies$land, print.progress = F)    #GPA-alignment
## ----PCA_example ----
PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy)
plot(PCA, main = "PCA", pch = 21, bg = "red")
## ----Phylomorph_example ----
plot(PCA, phylo = TRUE, pch = 21, bg = "red",
main = "Phylomorphospace")
## ----phyloPCA_example ----
phylo.PCA <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy, GLS = TRUE)
plot(phylo.PCA, phylo = TRUE, pch = 21, bg="red",
main = "phylogenetic PCA")
## ----PaCA_example ----
PaCA.gls <- gm.prcomp(Y.gpa$coords, phy = plethspecies$phy,
align.to.phy = TRUE, GLS = TRUE)
plot(PaCA.gls, phylo = TRUE, pch = 21, bg = "red",
main = "PaCA: Phylogenetically-Aligned Component Analysis")
## ----read_data ----
library(RRPP)
library(geiger)
library(phytools)
tree.best<-read.nexus("../data/Consensus of 1000 salamander trees.nex") #Maximum Credible Tree
plethdata<-read.csv("../data/meandata-CinGlutOnly.csv",row.names=1, header=TRUE )
svl<-plethdata[,2];names(svl)<-row.names(plethdata)
groups<-as.factor(plethdata[,1]); names(groups)<-row.names(plethdata)
Y<-as.matrix(plethdata[,c(3:8)])  ##This is our multivariate data
plethtree<-treedata(phy = tree.best,data = plethdata, warnings = FALSE)$phy
plot(plethtree)
axisPhylo(1)
PCA<-gm.prcomp(Y,phy=plethtree)   #principal components of Y
plot(PCA, pch=21,bg="black") ##we can put the PCA directly into the plot function
## ----phylo_reg----
PhyCov <- vcv.phylo(plethtree) #generate phylogenetic covariance matrix
rdf <- rrpp.data.frame(Y=Y, svl=svl,groups=groups,Cov = PhyCov)  #RRPP data frame
PGLS.reg <- lm.rrpp(Y ~ svl, Cov = PhyCov, data = rdf, iter = 999,
print.progress = FALSE,) # randomize residuals
anova(PGLS.reg)
## ---- phylo_ANOVA----
PGLS.aov<-lm.rrpp(Y ~ groups, Cov = PhyCov, data = rdf, iter = 999, print.progress = FALSE) # randomize residuals
anova(PGLS.aov)  #no difference once phylogeny considered
## ---- phylo_PLS ----
PLS.Y <- phylo.integration(A = Y[,1:3], A2 = Y[,4:6], phy= plethtree, print.progress = FALSE)
summary(PLS.Y)
plot(PLS.Y)
## ----phylo_signal----
PS.shape <- physignal(A=Y,phy=plethtree,iter=999, print.progress = FALSE)
summary(PS.shape)
plot(PS.shape)
## ----dim_phylo_signal----
physignal.eigen(A = Y, phy = plethtree)
Y
## ----dim_phylo_signal----
physignal.eigen(Y = Y, phy = plethtree)
## ----dim_phylo_signal----
PS.dim <- physignal.eigen(Y = Y, phy = plethtree)
PS.dim
plot(PS.dim)
## ----evol_rates----
ER<-compare.evol.rates(A=Y, phy=plethtree,gp=groups,iter=999, print.progress = FALSE)
summary(ER)   #significantly higher rate of morphological evolution 'large' Plethodon
plot(ER)
var.gp <- c("B","A","A","B","B","B")  #head (A) vs. body (B)
EMR<-compare.multi.evol.rates(A=Y,gp=var.gp, Subset=TRUE, phy= plethtree,iter=999, print.progress = FALSE)
summary(EMR) #Limb traits evolve faster than head traits
plot(EMR)
## ----pleth_read ----
data("plethspecies")
pleth_tree<-plethspecies$phy
landmarks<-plethspecies$land
procD_landmarks <- gpagen(landmarks)
install.packages("mvSLOUCH")
library(mvSLOUCH)
?mvSLOUCH
groups
## ----evol_modes----
regimes <- groups
?BrownianMotionModel
BM.pleth <- BrownianMotionModel(phyltree = plethtree, mData = Y)
BM.pleth
BM.pleth$ParamSummary
summary(BM.pleth)
OU.pleth <- mvslouchModel(phyltree = plethtree, mData = Y, regimes = regimes)
OU.pleth <- mvslouchModel(phyltree = plethtree, mData = Y,
regimes = regimes, kY = ncol(Y))
OU.pleth <- mvslouchModel(phyltree = plethtree, mData = Y,
regimes = regimes, kY = (ncol(Y)-1))
## ----evol_modes----
regimes <- as.vector(groups)
regimes
OU.pleth <- mvslouchModel(phyltree = plethtree, mData = Y,
regimes = regimes, kY = (ncol(Y)-1))
BM.pleth$ParamSummary$LogLik
phyltree<-ape::rtree(5)
phyltree<-phyltree_paths(phyltree)
regimes<-c("small","small","large","small","small","large","large","large")
regimes
gp.anc<-ace(x = groups, phy = plethtree,
type = "discrete", method = 'ML')
gp.anc
groups
colorkey <- c("black", "red")
names(colorkey) <- colnames(gp.anc$lik.anc) ##assign each character state a color
tip_cols<-colorkey[as.character(groups)] ##putting the trait values in as.character() is important!
plot(plethtree)
nodelabels(pie=gp.anc$lik.anc,piecol=colorkey,cex=0.5)
tiplabels(pch=19,col=tip_cols)
legend(x='bottomleft',legend = names(colorkey),fill=colorkey)
gp<-setNames(groups,names(groups))
gp
groups
tree.smp<-make.simmap(plethtree,groups,nsim=100)
anc.smp<-summary(tree.smp,plot=FALSE)
plot(anc.smp, fsize=0.8,ftype="i")
tree.smp
anc.smp$count
anc.smp$tips
gp.anc$lik.anc
branch.regime <- sapply(gp.anc$lik.anc, function(x) names(which.max(x)))
branch.regime
gp.anc$lik.anc
branch.regime <- apply(gp.anc$lik.anc, 1, which.max)
branch.regime
branch.regime <- apply(gp.anc$lik.anc, 1, names(which.max))
branch.regime <- apply(gp.anc$lik.anc, 1, function(x) names(which.max(x)))
branch.regime
OU.pleth <- mvslouchModel(phyltree = plethtree, mData = Y,
regimes = branch.regime, kY = (ncol(Y)-1))
branch.regime <- as.vector(apply(gp.anc$lik.anc, 1,
function(x) names(which.max(x))))
OU.pleth <- mvslouchModel(phyltree = plethtree, mData = Y,
regimes = branch.regime, kY = (ncol(Y)-1))
plethtree$edge
length(branch.regime)
gp.anc$lik.anc
anc.states <- colnames(gp.anc$lik.anc)[max.col(gp.anc$lik.anc)]
anc.states
branch.regime
all.states <- c(as.character(groups), anc.states)
branch.regime <- all.states[plethtree$edge[,2]]
branch.regime
OU.pleth <- mvslouchModel(phyltree = plethtree, mData = Y,
regimes = branch.regime, kY = (ncol(Y)-1))
colorkey <- c("black","red")
names(colorkey) <- colnames(gp.anc$lik.anc)
tip_cols<-colorkey[as.character(groups)]
plot(plethtree)
nodelabels(pie=gp.anc$lik.anc,piecol=colorkey,cex=0.5)
tiplabels(pch=19,col=tip_cols)
legend(x='bottomleft',legend = names(colorkey),fill=colorkey)
#Compare
BM.pleth$ParamSummary$LogLik
OU.pleth$MaxLikFound
OU.pleth
OU.pleth$FinalFound$LogLik
#Compare
BM.pleth$ParamSummary$aic
BM.pleth$ParamSummary$aic
OU.pleth$FinalFound$ParamSummary$aic
#Compare
BM.pleth$ParamSummary$aic
OU.pleth$FinalFound$ParamSummary$aic
setwd("C:/DeanData/Teaching/Biol-465-EEOB 565-Macroevolution/MacroevolutionPracticals/13-MultivariatePCMs/scripts")
install.packages("BioGeoBEARS")
devtools::install_github('nmatzke/BioGeoBEARS')
ant.tree <- read.tree("../data/attine-tree-pruned.tre")
## ----read_data ----
library(phytools)
library(biogeobears)
library(BioGeoBEARS)
ant.tree <- read.tree("../data/attine-tree-pruned.tre")
print(ant.tree, prinlen = 2)
## ----read_data ----
library(phytools)
library(BioGeoBEARS)
ant.tree <- read.tree("../data/attine-tree-pruned.tre")
print(ant.tree, prinlen = 2)
ant.data <- getranges_from_LagrangePHYLIP(lgdata_fn = "attine-distribution-data.txt")
ant.data <- getranges_from_LagrangePHYLIP(lgdata_fn = "../data/attine-distribution-data.txt")
ant.data <- getranges_from_LagrangePHYLIP(lgdata_fn =
"../data/attine-distribution-data.txt")
head(ant.data)
ant.data
print(ant.data, prinlen = 4)
print(ant.data, prinlen = 2)
class(ant.data)
writelines(readLines("../data/attine-distribution-data.txt",4))
writeLines(readLines("../data/attine-distribution-data.txt",4))
## ----plot_data ----
tmp <- ant.data@df
tmp[,1:5] <- lapply(tmp[,1:5], factor)
colnames(tmp) <- c("Nearctic","Middle America",
"South America", "Afrotropics",
"Australasia")
colors <- setNames(replicate(ncol(tmp),
setNames(c("white", "darkgray"),0,1),
simplify = FALSE), colnames(tmp))
colors <- setNames(replicate(ncol(tmp),
setNames(c("white", "darkgray"),0:1),
simplify = FALSE), colnames(tmp))
object <- plotTree.datamatrix(ant.tree,tmp,fsize=0.5,
yexp=1.1, header=TRUE, xexp=1.25,colors = colors)
legend("topleft", c("species absent","species present"),
pcs=22, pt.bg=c("white","darkgray"), pt.cex=1.3,cex=0.8,btw="n")
legend("topleft", c("species absent","species present"),
pcx=22, pt.bg=c("white","darkgray"), pt.cex=1.3,cex=0.8,btw="n")
legend("topleft", c("species absent","species present"),
pch=22, pt.bg=c("white","darkgray"), pt.cex=1.3,cex=0.8,btw="n")
legend("topleft", c("species absent","species present"),
pch=22, pt.bg=c("white","darkgray"), pt.cex=1.3,cex=0.8,bty="n")
## ----run_dec ----
max_range_size <- 2
bgb_run <- define_BioGeoBEARS_run(
num_cores_to_use = 1,
max_range_size = max_range_size,
trfn = "../data/attine-tree-pruned.tre",
return_condlikes_table = TRUE)
bgb_run$geogfn <- "../data/attine-distribution-data.txt"
check_BioGeoBEARS_run(bgb_run)
DEC.fit <- bears_optim_run(bgb_run)
?bears_optim_run
DEC.fit$optim_result
print(ant.tree, prinlen = 2)
ant.data <- getranges_from_LagrangePHYLIP(lgdata_fn =
"../data/attine-distribution-data.txt")
writeLines(readLines("../data/attine-distribution-data.txt",4))
